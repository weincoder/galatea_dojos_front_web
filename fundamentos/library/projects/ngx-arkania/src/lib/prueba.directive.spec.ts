import { PruebaDirective } from './prueba.directive';

describe('PruebaDirective', () => {
  it('should create an instance', () => {
    const directive = new PruebaDirective();
    expect(directive).toBeTruthy();
  });
});
